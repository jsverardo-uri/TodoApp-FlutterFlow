{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.com/schemas/tasks.schema.json",
  "title": "Tasks Collection Schema",
  "description": "JSON Schema for documents in the Firestore tasks collection.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "completed",
    "created",
    "details",
    "title",
    "user"
  ],
  "properties": {
    "completed": {
      "type": "boolean"
    },
    "created": {
      "type": "string",
      "format": "date-time",
      "description": "Firestore timestamp represented as an RFC 3339 / ISO 8601 date-time string."
    },
    "details": {
      "type": "string"
    },
    "title": {
      "type": "string",
      "minLength": 1
    },
    "user": {
      "type": "string",
      "pattern": "^/users/[A-Za-z0-9_-]+$",
      "description": "Firestore document reference path string pointing to a user document."
    }
  }
}